services:
  falkordb-mcpserver:
    image: falkordb/mcpserver:${FALKORDB_MCP_VERSION:-1.2.2}
    container_name: falkordb-mcpserver
    restart: unless-stopped
    ports:
      - "${FALKORDB_MCP_PORT:-3005}:3000"
    environment:
      - NODE_ENV=production
      - MCP_TRANSPORT=${MCP_TRANSPORT:-http}
      - MCP_PORT=3000
      - MCP_API_KEY=${MCP_API_KEY:-}
      - FALKORDB_HOST=falkordb
      - FALKORDB_PORT=6379
      - FALKORDB_USERNAME=${FALKORDB_USERNAME:-}
      - FALKORDB_PASSWORD=${FALKORDB_PASSWORD:-}
      - FALKORDB_DEFAULT_READONLY=${FALKORDB_DEFAULT_READONLY:-false}
      - FALKORDB_STRICT_READONLY=${FALKORDB_STRICT_READONLY:-false}
      - ENABLE_FILE_LOGGING=${ENABLE_FILE_LOGGING:-false}
    depends_on:
      falkordb:
        condition: service_healthy
    networks:
      - falkordb-network

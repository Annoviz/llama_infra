FROM python:3.12-slim-bookworm

WORKDIR /app
COPY server.py .

# FastMCP with streamable-http transport + async HTTP client for Gotenberg API
RUN pip install --no-cache-dir "mcp>=1.0,<2.0" httpx

EXPOSE 8000

CMD ["python", "server.py"]
